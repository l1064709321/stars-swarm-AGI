[workspace]
resolver = "2"
members = [
    "core",       # 防御核心（Tarpit + SNN + Kalman）
    "star_os",    # 十层神经架构AI系统（MoE + Mamba + burn）
]

[workspace.package]
version = "0.0.1"
edition = "2021"
license = "MIT"

[workspace.dependencies]
serde = { version = "1.0", features = ["derive"] }
rand = "0.8"
